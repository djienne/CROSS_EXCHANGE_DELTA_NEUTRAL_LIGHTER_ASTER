# Core dependencies
python-dotenv>=1.0.0
aiohttp>=3.9.0
websockets

# Ethereum/Web3 dependencies (for Aster request signing)
web3>=6.0.0
eth-account>=0.10.0
eth-abi>=4.0.0

# Lighter SDK
git+https://github.com/elliottech/lighter-python.git

# NOTE: `asyncio` was pinned here (asyncio>=3.4.3). That PyPI package is a backport for
# Python 3.3 and installing it on 3.12 shadows the standard library module with a decade-
# old copy. Removed deliberately - do not add it back; asyncio is part of the stdlib.
#
# numpy, pandas, scipy and colorama were also listed and are imported nowhere in this
# project. Together they added several hundred MB to the image for nothing.
